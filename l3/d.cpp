#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main() {
    string buffer;
    cin >> buffer;

    int target = stoi(buffer);
    vector<int> n;
    for (char i : buffer) n.push_back((int) i);

    int t = 0;
    
    return 0;
}